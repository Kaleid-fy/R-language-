1.读入的数据文件的要与给的文件格式相同，
  都应该为.csv文件。

2.表不要加表示序号的行名，如1,2,3...

3. nodes表中第一列是必须的，edges表中前2列是必须的

4.代码只计算了一些常用的社交网络分析的图统计量，
  igraph包还提供了其他的统计量计算函数。

5.函数说明部分，由于自己不是很懂，直接把R中的说明整理了一下贴上去了，
  怕翻译错了产生误导。